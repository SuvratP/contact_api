<!DOCTYPE html>
<html>
<head>
  <title>Edit Contact</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <div class="container">
    <h2>Edit Contact</h2>
    <form method="POST" action="/edit/<%= contact._id %>">
      <input name="name" value="<%= contact.name %>" required><br>
      <input name="email" value="<%= contact.email %>" required><br>
      <input name="phone" value="<%= contact.phone %>" required><br>
      <input name="type" value="<%= contact.type %>" required><br>
      <button type="submit">Update Contact</button>
    </form>
  </div>
</body>
</html>
